import { Placeholder } from "../_placeholders";
export default function ModulesPage() {
  return (
    <Placeholder
      session="Session 2"
      title="My Modules"
      body="Module View (Screen 02) wires here. The schema already holds Module 0 and its 14 tasks — lesson cards, task list with planter-only enforcement, and worksheet rail come next."
    />
  );
}
