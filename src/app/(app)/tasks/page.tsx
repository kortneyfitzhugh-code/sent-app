import { Placeholder } from "../_placeholders";
export default function TasksPage() {
  return (
    <Placeholder
      title="All Tasks"
      body="The unified task view across modules. Planter-only enforcement runs in the database — this page reads it."
    />
  );
}
