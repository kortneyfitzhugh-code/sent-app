import { Placeholder } from "../_placeholders";
export default function TasksPage() {
  return (
    <Placeholder
      session="Session 2"
      title="All Tasks"
      body="Unified task view across modules. Planter-only enforcement already lives in the database (trg_enforce_planter_only); this page reads it and lets the planter assign team-eligible tasks."
    />
  );
}
